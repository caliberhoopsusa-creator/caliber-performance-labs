// Alias of generate-mockup — same behavior, separate route so the regenerate
// button is semantically distinct.
export { POST } from "../generate-mockup/route";
export const runtime = "nodejs";
export const maxDuration = 60;
